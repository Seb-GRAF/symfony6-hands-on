{{ form_start(form) }}
<div>{{ form_errors(form)}}</div>

<div>{{ form_label(form.title, 'Please enter the title') }}</div>
<div>{{ form_widget(form.title) }}</div>

<div>{{ form_label(form.text, 'Please enter your post text') }}</div>
<div>{{ form_widget(form.text, {'attr': {'style':'color:gray'}}) }}</div>

<div>
  <button type='submit'>Save</button>
</div>
{{ form_end(form) }}
